name: validate

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -e ".[dev]"
      - run: python -m pytest -q --basetemp=.pytest-ci-${{ matrix.os }}-${{ matrix.python-version }}
      - run: anyang-project validate projects
      - run: anyang-loop validate projects
      - run: anyang-project validate-interfaces
      - run: anyang-project validate-artifacts
      - run: anyang-project validate-agency
      - run: anyang-project validate-epistemics
      - run: anyang-ops privacy-scan --repo .
