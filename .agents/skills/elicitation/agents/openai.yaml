interface:
  display_name: "Elicitation"
  short_description: "Resolve consequential unknowns with low-load questions"
  default_prompt: "Read and follow the canonical $elicitation skill."
policy:
  allow_implicit_invocation: true
