interface:
  display_name: "Executive Audit"
  short_description: "Select, run, and reconcile bounded repository audits"
  default_prompt: "Use $executive-audit to select, prepare, or reconcile a bounded, read-only repository audit."
